#include <stdio.h> 
int main(){

float a,b, c, diferente, perimetro, area; 

scanf("%f %f %f", &a, &b, &c);
 a- b= diferente;
if (diferente >= c )
{
   area = (a + b) * c / 2 ;
    printf("Area = %.1f", area);
}
else 
{
    a + b + c = perimetro;

    printf("Perimetro = %.1f",perimetro);
}



    return 0;
}